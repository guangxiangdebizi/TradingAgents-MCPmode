# MCP工具与大模型交互核心依赖
langchain-openai>=0.2.0
langchain-mcp-adapters==0.1.0
langgraph>=0.1.0
python-dotenv>=1.0.0

# Web前端依赖
streamlit>=1.28.0
streamlit-option-menu>=0.3.6

# 智能体框架依赖
langchain>=0.3.0
langchain-core>=0.3.36
langchain-community>=0.3.0

# 数据处理和工具
pandas>=1.5.0
numpy>=1.24.0
requests>=2.28.0
aiohttp>=3.8.0

# 配置和类型支持
pydantic>=2.0.0
typing-extensions>=4.5.0

# 异步支持
asyncio-mqtt>=0.13.0
# uvloop>=0.17.0  # 仅支持Unix系统，Windows用户请注释此行

# 导出工具依赖 (用于生成分析报告)
# 核心导出依赖
markdown2>=2.4.0     # Markdown生成 (比markdown库更快更稳定)
python-docx>=0.8.11  # DOCX文档生成
reportlab>=4.0.0     # PDF生成 (备选方案)

# PDF生成首选依赖 (Windows可能需要额外配置)
weasyprint>=61.0     # PDF生成首选方案，支持CSS样式
# 注意：Windows用户如遇到WeasyPrint问题，可注释上行，仅使用reportlab

# 开发工具
pytest>=7.0.0
pytest-asyncio>=0.21.0
black>=23.0.0
flake8>=6.0.0
# Markdown to PDF Converter Dependencies
# 安装命令: pip install -r md_to_pdf_requirements.txt

# 主要依赖
markdown2>=2.4.0
pdfkit>=1.0.0

# 备用方案依赖（可选）
reportlab>=4.0.0
beautifulsoup4>=4.12.0

# 代码高亮（可选）
pygments>=2.15.0